<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Card</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black/70 flex-wrap">
    <h1 class="text-center mb-[5vw] text-[2vw] text-white mt-[2vw]">All Users :</h1>
    <h1 class="text-center text-blue-500 text-[1.5vw]"><a href="/">Back To Home</a></h1>
   <div class="users flex flex-wrap gap-[1vw] p-10">
    <% users.forEach((user) => { %>
        <div class="card bg-white/30 rounded-lg p-8 max-w-xs shadow-lg">
            <div class="flex flex-col items-center">
                <div class="h-52 w-[13vw] bg-green-300 rounded-lg overflow-hidden">
                    <img class="h-full w-full object-cover" src="<%= user.imageUrl %>" alt="User Image">
                </div>
                <h1 class="text-white mt-4 text-lg font-semibold">Name: <span class=""><%= user.name %></span></h1>
                <h1 class="text-white mt-2">E-mail: <%= user.email %></h1>
                <h1 class="text-white mt-2">Age: <%= user.age %></h1>
               <div class="flex items-center mt-4 gap-4">
                    <button class="text-blue-500 border border-blue-500 px-4 py-1 rounded-lg bg-blue-500 text-white ">Edit</button>
                    <button class="text-red-500 border border-red-500 px-4 py-1 rounded-lg bg-red-500 text-white "> <a href="/delete/<%= user._id %>">Delete</a></button>
               </div>
            </div>
       </div>
    <% }) %>
    
   </div>
   
</body>
</html>
