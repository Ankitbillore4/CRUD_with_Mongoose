<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User Form</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-gray-900 via-black to-gray-900 flex flex-col justify-center items-center min-h-screen text-gray-200">
    <!-- Link to Read Updated Users -->
    <h1 class="text-2xl font-semibold text-blue-400 mb-8">
        <a href="./read.ejs" class="hover:underline">Read Updated Users</a>
    </h1>

    <!-- Form Container -->
    <form action="/update/<%= user._id %>" method="post" autocomplete="off"
        class="bg-white/10 shadow-lg rounded-lg px-10 py-8 w-full max-w-md backdrop-blur-lg border border-white/10">
        <h2 class="text-2xl font-semibold mb-6 text-center text-yellow-400">Update User Form</h2>

        <!-- Name Field -->
        <div class="mb-6">
            <label for="name" class="block text-gray-300 text-sm font-semibold mb-1">Name</label>
            <input type="text" id="name" name="name" placeholder="Enter your name" value="<%= user.name %>"
                class="w-full p-3 rounded-lg bg-gray-800 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-500 transition">
        </div>

        <!-- Email Field -->
        <div class="mb-6">
            <label for="email" class="block text-gray-300 text-sm font-semibold mb-1">Email</label>
            <input type="email" id="email" name="email" placeholder="Enter your email" value="<%= user.email %>"
                class="w-full p-3 rounded-lg bg-gray-800 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-500 transition">
        </div>

        <!-- Age Field -->
        <div class="mb-6">
            <label for="age" class="block text-gray-300 text-sm font-semibold mb-1">Age</label>
            <input type="number" id="age" name="age" placeholder="Enter your age" value="<%= user.age %>"
                class="w-full p-3 rounded-lg bg-gray-800 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-500 transition">
        </div>

        <!-- Image URL Field -->
        <div class="mb-6">
            <div class="flex justify-between items-center">
                <label for="image" class="text-gray-300 text-sm font-semibold">Image URL</label>
                <a href="https://www.google.com" target="_blank" class="text-sm font-bold text-yellow-400 flex items-center hover:underline">
                    <i class="ri-search-line mr-1"></i> Search Image
                </a>
            </div>
            <input type="url" id="image" name="imageUrl" placeholder="Enter Image URL" value="<%= user.imageUrl %>"
                class="w-full p-3 rounded-lg bg-gray-800 text-gray-100 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-500 transition">
        </div>

        <!-- Submit Button -->
        <div class="text-center mt-6">
            <button type="submit"
                class="w-full py-3 px-6 rounded-lg bg-yellow-500 hover:bg-yellow-600 text-gray-900 font-semibold focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yellow-500 transition">
                Update User
            </button>
        </div>
    </form>

</body>

</html>
